source "https://rubygems.org"

# Match the GitHub Pages dependency set used to publish this Jekyll site.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on modern Ruby versions.
gem "webrick", "~> 1.9"
